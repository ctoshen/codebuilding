version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
     # - sudo apt-get clean && rm -rf /var/lib/apt/lists/*
     # - sudo apt-get update
     - sudo apt-get install jq
     -jq --verson
